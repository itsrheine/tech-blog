<article class="card">
    <div class="title">
        <a href="/post/{{id}}" target="_blank">{{title}}</a>
        <span>Post created by {{user.username}} on {{created_at}}</span>
    </div>
    <div class="card-body">{{content}}</div>
</article>