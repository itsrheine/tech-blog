<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-dark">
            {{#if loggedIn}}
            <a class="navbar-brand text-white" href="/"><strong>My Dashboard</strong></a>
            {{else}}
            <a class="navbar-brand text-white" href="/"><strong>Tech Blog</strong></a>
            {{/if}}
            <ul class="navbar-nav mr-auto ">
                <li class="nav-item">
                    {{#if loggedIn}}
                    {{else}}
                    <a class="nav-link text-white" href="/signup">Sign Up</a>
                    {{/if}}
                </li>
                <li class="nav-item">
                    {{#if loggedIn}}
                    <a class="nav-link text-white" href="#" id="logout">Logout</a>
                    {{else}}
                    <a class="nav-link text-white" href="/login">Login</a>
                    {{/if}}
                </li>
            </ul>
        </nav>
    </header>

    <div class="hero bg-light">
        {{#if loggedIn}}
        <h1 class="lead text-center d-flex justify-content-center">Jot and Inspire</h1>
        <p class="lead d-flex justify-content-around">Write a blog today!</p>
        {{else}}
        <h1 class="display-4 text-center">Write it out loud!</h1>
        <p class="lead text-center">Share your ideas and explore new ideas through blogging.</p>
        <p class="lead text-center">Sign up today!</p>
        {{/if}}
    </div>

    <main>
        {{#if loggedIn}}
        <div class="bg-secondary">
            <nav class="navbar navbar-expand-lg bg-secondary">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link justify-content-sm-start text-white nav-pill" data-toggle="pill"
                            href="/dashboard/new">Create New Post</a>
                    </li>
                </ul>
            </nav>
            {{{ body }}}
        </div>
        {{else}}
        {{{ body }}}
        {{/if}}
    </main>

    <footer>
        <div class="footer-copyright text-center py-3">© <a href="http://github.com/itsrheine">itsrheine</a></div>
    </footer>
    </div>
    {{#if loggedIn}}
    <script src="javascript/logout.js"></script>
    {{/if}}

</body>

</html>